import { Box, styled } from "@mui/material";

export const TabsMain = styled(Box)(({ theme }) => ({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    gap: '2rem',
    marginTop: '2rem',
    marginBottom: '2rem',
    width: '95%',
}));